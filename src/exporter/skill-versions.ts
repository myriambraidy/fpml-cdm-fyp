/** Align with `Skill.name` in each skill's `index.ts` under `src/skills/`. */
export const SKILL_VERSIONS: Record<string, string> = {
  party_resolver: '1.0.0',
  temporal_mapper: '1.0.0',
  cardinality_checker: '1.0.0',
  ir_swap_resolver: '0.1.0',
  floating_rate_index_resolver: '1.0.0',
  unit_normalizer: '1.0.0',
}
